# Temperature Converter (Celsius to Fahrenheit)

This is a simple Python-based web application that converts temperatures from Celsius to Fahrenheit. It uses Flask for the backend and provides a simple HTML interface to interact with the application.

## Prerequisites

Before running the application, make sure you have the following installed on your machine:

- [Python}

## Setup

Follow these steps to clone the repository and set up the application on your local machine:

### 1. Clone the repository

Open a terminal or command prompt and run the following command to clone the repository:

```bash
git clone https://github.com/CinthiaDayanara/temperature.git
